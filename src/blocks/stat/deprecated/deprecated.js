// Import earlier versions of the block.
import { config as v1 } from './v1/v1_stat_child_block.js';

// Setup an array of deprecated versions for the bu/stat child block.
const deprecatedStatChildBlock = [ v1 ];

export default deprecatedStatChildBlock;
