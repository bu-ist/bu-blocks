# Headline (v1)

This is a deprecated version of the Headline block. The original version of the Headline block was a static block and stored some of the attributes in the saved HTML. This version is now a dynamic block that stores the attributes in the block's `attributes` property.